const announcement = require('./announcement');
const request = require('./request');
const user = require('./user');
const devTeam = require('./devTeam');

module.exports = {
  request, announcement, user, devTeam,
};
